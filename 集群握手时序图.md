
~~~
sequenceDiagram
    participant Client as 客户端 (Client)
    participant NodeA as 节点A (A)
    participant NodeB as 节点B (B)

    Client ->> NodeA: 1.输入命令 Cluster Meet host port
    NodeA ->> NodeA: 2.创建NodeB(此时的节点名称是随机的)实例添加到Cluster(REDIS_NODE_HANDSHAKE|REDIS_NODE_MEET)
    NodeA ->> NodeB: 3.创建socket并发送连接请求到NodeB
    NodeB ->>NodeB: 4.NodeB持有NodeA的连接通道(link->node -- Null, 集群不存在NodeA)
    NodeA ->>NodeA: 5.创建link (link->node -- nodeB, 集群存在nodeB)
    NodeA ->> NodeB: 6.向NodeB发送MEET类型消息
    NodeB ->> NodeB: 7.NodeB创建NodeA节点对象并添加到集群(REDIS_NODE_HANDSHAKE)
    NodeB ->> NodeA: 8.NodeB向NodeA发送PONG类型消息 (此时带着正确的节点B名称)
    NodeA ->> NodeA: 9.NodeA按照pong消息中带的真实名称对NodeB节点实例进行重命名 B节点握手完毕
    NodeB ->> NodeA:10节点B的clusterCron中会向NodeA发起连接请求
    NodeA ->> NodeA:11.NodeA持有NodeB的连接通道
    NodeB ->> NodeA: 12.NodeB向NodeA发送PING消息
    NodeA ->> NodeB: 13.NodeA回复NodeAPONG消息
    NodeB ->> NodeB: 14.NodeB按照pong消息中带的真实名称对NodeA节点实例进行重命名 A节点握手完毕

~~~